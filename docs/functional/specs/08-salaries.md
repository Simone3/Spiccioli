# §8 — Salaries

*[Index](../README.md) · [why it is this way](../why/08-salaries.md)*

Two tabs: Payslips, Contracts. Scoped to one contract, then to one year.

---

## 8.1 Payslips

- **Contract selector** scopes the whole screen. Every table and chart covers the selected contract only. **No figure anywhere sums across contracts**, and there is no cross-contract comparison.
- The **Contracts tab** beside it is where contracts are created and edited ([§4.3](04-accounts.md#43-creating-and-editing)).
- **Per-year table** is always visible and complete; selecting a row swaps the payslip table below it. Its columns are the year, its **payslip count**, annual contract gross, total gross, total net salary, working days and the two hourly figures ([§11.7](11-calculations.md#117-salary-figures)) — totals and hourly rates, never averages. `Working days` is the one cell here that leads anywhere: it opens a form holding that single field, and that form **is** the ContractYear record — a number creates it and **saving the form empty deletes it**, which puts the year back exactly where it was before anything was entered. That delete is the one that is not confirmed ([§13](13-validation.md)), the form being the whole of the record.
- **One row per calendar year from the contract's start year to its end year**, or to the current year while the contract is running, ascending. **The rows come from the contract's own dates and not from the payslips**: a year in the middle with nothing recorded in it is a year that shows zeros and an empty working-days cell.
- **A year with no ContractYear record shows an empty working-days cell**, and the two hourly columns read *undefined* rather than zero or a dash ([§11.7](11-calculations.md#117-salary-figures)). Type the number and the row completes itself.
- **Working days is always the whole calendar year**, never the part worked. In a partial first or last year the hourly figures are consequently understated, and that is accepted rather than corrected.
- **Payslip table** shows every payslip of the selected year, by the month the pay is for. A month may hold several; the *tredicesima* is a second December row with a label, not a synthetic thirteenth month.
- **Its columns are the month, the label, then every stored figure on the payslip in the order [§2](02-domain-model.md) lists them** — contract gross, gross, net payment, refunds, car, and the three pension figures — and then notes. **The three pension figures share one column, `Pension fund`**, because they are three credits into one place and the column that holds them says which place that is, where `Employee`, `Employer` and `TFR` standing on their own do not. The cell writes them as **employee + employer + TFR** where any of the three is more than zero, and as a single **€ 0,00** where none of them is — a payslip with nothing under any of the three headings, stated once rather than three times. **The one derived column is inserted rather than appended**: **net salary** ([§11.7](11-calculations.md#117-salary-figures)) sits immediately after `carPayment`, beside the three entered figures it is made of, which is the one departure from [§2](02-domain-model.md)'s order and the only one. **Ten columns**, which is every field of the record except `id`, `contractId`, carried by the selector above, and `year`, carried by the row selected in the per-year table, with the three pension figures counted once.
- **Ordered by month ascending, then by label alphabetically with the unlabelled row first.** The table is scoped to one year, so the year is not a key here; the two places payslips are walked across years — the matchers of [§11.6](11-calculations.md#116-derived-matching) and the entry lists of [§9](09-checks.md) — put the year in front of these two.
- **`Gross` is the payslip's own *totale lordo* line, typed as printed** ([§2](02-domain-model.md)), and it is the only column here whose meaning comes from the document rather than from this specification. **Nothing on this screen divides a net figure by it** ([§15](15-out-of-scope.md)): everything that reads `gross` — its total on the per-year table, the averages chart, `Gross / hour` — is a sum, a mean or a rate over that one field, so whatever the employer's line contains, those figures agree with each other and across years ([§11.7](11-calculations.md#117-salary-figures)).
- **`Net payment` may be negative**, alone among the figures on this screen ([§13](13-validation.md)): a December whose year-end tax recalculation exceeds the month's net is a real payslip and is entered as printed. `Net salary` follows it down, and check 4 looks for a debit of that amount or reports the payslip when the employer carried the debt forward instead ([§9](09-checks.md)).
- A payslip's money often reaches the bank the following month. The screen does not care — every figure here comes from the payslips — but checks 4 and 5 do, and they look one month ahead ([§11.6](11-calculations.md#116-derived-matching)).
- **Two charts, side by side, covering every year of the contract.** *Average per month, per year* plots `yearAvgGross` and `yearAvgNet` ([§11.7](11-calculations.md#117-salary-figures)); *Totals per year* plots total gross, total net salary and the contract line, *contract × months*, which uses the contract's own `monthsPerYear` and is drawn dashed because it is a term rather than a measurement.
- **Three windows over the pair: *Last 5*, *Last 10* and *All*.** They are counts of years rather than spans of months, because a point here *is* a year: a window keeps the last five rows of the per-year table, the last ten, or every row there is. **They are the years filter of [§6.1](06-categories.md#61-report)'s kind and not the net worth chart's** ([§3.1](03-portfolio.md#31-behaviour)) — a line of yearly points has nothing to show over a single year — and like that filter this is a picker over a closed set and not a range.
- **The window is the pair's, and each chart carries the switch.** **The two charts are read across**: an average against a total, and both against the contract line, so a window applying to one of them and not the other would be putting two periods side by side and calling it a comparison — **moving either switch moves both**, and there is never a moment at which the two charts are showing different years. **It is drawn on each card rather than once above them** so that it is always beside the chart it governs, including where the window is narrow and the two cards stack one under the other; the cost is that a reader has to see the far switch move to learn that the two are one, and that is paid once.
- **It opens on *Last 5*** and is found on whatever it was left on for as long as the file is open ([§12.2](12-storage.md#122-the-menu-bar-and-which-file-is-open)). **A contract shorter than the window shows every year it has**, so a contract of five years or fewer draws the same two charts under all three — and the three are offered all the same, a control that came and went with the length of a contract being harder to find than one that sometimes says the same thing three ways.
- **The window is the charts' and nothing else's.** The per-year table below is complete and stays complete, the payslip table stays scoped by the row selected in it, and **no figure anywhere changes** ([§11.7](11-calculations.md#117-salary-figures)): what a window changes is how many of the same points are drawn. What it buys is the value axis — five years of pay drawn against ten years of scale is a flat line saying nothing.
- A payslip is corrected on the form it was recorded on, opened by **Edit** in its row menu, which also holds **Duplicate** and **Delete**. The fields are the same either way and so is the record. Duplicate copies every field including `year`, `month` and `label` — a month may legitimately hold more than one payslip ([§2](02-domain-model.md)), so the copy is valid where it lands. **Add payslip** sits in the screen's heading and is offered whatever the year below holds, an empty year offering it a second time in its own empty state ([§14](14-empty-and-error-states.md)). It opens that same form with **all twelve stored fields**, `year` at the top and the other eleven below it — **the three pension figures last, under a heading naming the fund they are credits into**, so that nothing on the form stands under that heading which is not one of them. **Year is the one field that arrives filled in**, and it is a picker holding exactly the years the per-year table has rows for — the contract's own years, so no year the contract never covered can be chosen. **It opens on the year whose row is selected in that table**, which is the year the payslip table below is already showing; **a row is always selected** — on first arriving at the screen it is the contract's last year, and on coming back to it the row that was selected when it was left ([§12.2](12-storage.md#122-the-menu-bar-and-which-file-is-open)) — so the field always opens on something. Changing it is how a payslip is filed into another year without leaving the form, and **saving into a year other than the selected one moves the per-year selection to that year**, so the new row is visible where it landed rather than in a table it is not in. **Every other field is empty**: no month is proposed, no figure is carried over from the previous payslip, and **no amount on the form is filled in on the user's behalf** ([§5.5](05-transactions.md#55-add-transaction)). Duplicate is what exists for a row that resembles another.

## 8.2 Contracts

- One row per employer, **ordered by `startDate`, then by name** to break the tie.
- **Months per year** and **hours per day** are contract terms that every figure on the other tab divides by, which is why the two tabs sit together: this is the denominator, that is the numerator. **A new contract opens on 12 and on 8,00**, the ordinary year and the ordinary day; a contract carrying extra months of pay is typed over the twelve.
- **A contract is never retired, only ended.** Filling in `endDate` records when it finished; it stays in the selector, keeps its payslips and keeps its years. There is nothing to exclude from a total, because no figure in the application sums across contracts ([§8.1](#81-payslips)).
- **The dates cannot be narrowed past the records that depend on them.** The form refuses it and names what is in the way ([§13](13-validation.md)). Widening is always fine, and is what an employer extending a contract looks like.
- **Payslips** counts what points at the contract, and is what decides whether it can be deleted: at zero it can, otherwise it cannot ([§13](13-validation.md)). Its ContractYear records go with it.
- Changing `monthsPerYear` or `hoursPerDay` re-computes every derived figure on the Payslips tab for every year, **including years already closed**. There is no history of contract terms — the current values are applied to the whole contract.

---

[← §7 Investments](07-investments.md) · [§9 Checks →](09-checks.md)
